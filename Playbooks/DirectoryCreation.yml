---
- hosts: all
  remote_user: ansible
  tasks:
  - name: Direcctory creation
    file:
      path: /opt/web
      state: directory
      owner: ansible
      group: ansible
      mode: 0777
    become: yes
    become_method: sudo

